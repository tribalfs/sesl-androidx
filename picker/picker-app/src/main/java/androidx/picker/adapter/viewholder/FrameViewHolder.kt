package androidx.picker.adapter.viewholder

import android.view.View

/**
 * Represents a frame view holder for use as Header or Footer.
 *
 * This class extends the [PickerViewHolder] class and is responsible for holding and managing
 * the views related to a frame item in the picker.
 *
 * @param view The view associated with the frame item.
 */
class FrameViewHolder(view: View) : PickerViewHolder(view)